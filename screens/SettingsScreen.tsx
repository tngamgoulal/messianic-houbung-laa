import React, { useState } from 'react';\nimport { View, Text, Switch, Slider } from 'react-native';\n\nconst SettingsScreen = () => {\n  const [isDarkMode, setIsDarkMode] = useState(false);\n  const [fontSize, setFontSize] = useState(14);\n\n  const toggleSwitch = () => {\n    setIsDarkMode(previousState => !previousState);\n  };\n\n  return (\n    <View style={{ flex: 1, padding: 20, backgroundColor: isDarkMode ? '#333' : '#FFF' }}>\n      <Text style={{ fontSize: fontSize, color: isDarkMode ? '#FFF' : '#000' }}>Settings</Text>\n      <View style={{ marginVertical: 20 }}>\n        <Text style={{ fontSize: fontSize, color: isDarkMode ? '#FFF' : '#000' }}>Dark Mode</Text>\n        <Switch\n          trackColor={{ false: '#767577', true: '#81b0ff' }}\n          thumbColor={isDarkMode ? '#f5dd4b' : '#f4f3f4'}\n          onValueChange={toggleSwitch}\n          value={isDarkMode} /\n        >\n      </View>\n      <View>\n        <Text style={{ fontSize: fontSize, color: isDarkMode ? '#FFF' : '#000' }}>Font Size: {fontSize}</Text>\n        <Slider\n          minimumValue={10}\n          maximumValue={30}\n          step={1}\n          value={fontSize}\n          onValueChange={value => setFontSize(value)} /\n        >\n      </View>\n    </View>\n  );\n};\n\nexport default SettingsScreen;